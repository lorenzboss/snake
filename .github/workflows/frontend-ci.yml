name: Frontend CI/CD Pipeline

on:
  workflow_call:

jobs:
  pipeline:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "frontend/package-lock.json"

      - name: Install dependencies
        run: npm ci

      - name: Run lint and format check
        run: npm run lint

      - name: Run tests
        run: npm test

      - name: Upload test reports
        if: success() || failure() # Always upload even if tests fail
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: frontend/test-results
          retention-days: 7

      - name: Build frontend
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: frontend/dist
          retention-days: 1
